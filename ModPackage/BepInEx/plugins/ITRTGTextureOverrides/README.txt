ITRTGTextureOverrides

Textures/*.png are replacement images.
texture_overrides.tsv maps Unity Resources.Load paths or asset names to PNG files.

If a texture does not replace in-game, enable LogMisses in BepInEx/config/itrtg.vegan-texture-overrides.cfg and add the discovered path to texture_overrides.tsv.
